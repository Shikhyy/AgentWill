{
  "name": "agentwill",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "deploy:contracts": "forge script script/Deploy.s.sol --rpc-url base --broadcast --verify",
    "node": "anvil"
  },
  "dependencies": {
    "foundry": "^5.0.0"
  }
}
